# The base URL of the deployed backend API (e.g. Render, Railway, AWS, etc.)
# If left empty, it will fall back to http://localhost:8000 for local development.
VITE_API_URL=https://your-backend-api-url.com

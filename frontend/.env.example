# Optional: base URL of an externally hosted backend API (e.g. Render, Railway).
# Leave unset to use the default behavior:
#  - local dev  -> http://localhost:8000 (run backend/main.py separately)
#  - production -> same-origin /api (served by the Vercel serverless function in frontend/api)
# VITE_API_URL=https://your-backend-api-url.com
